# Logistics_Database_Migration
This project exposes the student to cleaning and de-duplicating a logistics company operations data, splitting it into a normalized relational schema with enforced primary/foreign keys, and implementing date-based table partitioning in PostgreSQL for optimized transactional and analytical performance.
